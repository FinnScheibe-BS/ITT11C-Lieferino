# Vorlage für lokale Umgebungsvariablen (im Docker-Setup stehen die Werte in docker-compose.yml).
PORT=8080
DB_HOST=localhost
DB_PORT=5432
DB_USER=lieferino
DB_PASSWORD=lieferino
DB_NAME=lieferino
JWT_SECRET=bitte-aendern

# Für echten E-Mail-Versand (Gmail-App-Passwort, NICHT das normale Passwort):
GMAIL_USER=
GMAIL_APP_PASSWORD=
